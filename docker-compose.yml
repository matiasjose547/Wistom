version: '3.8'

services:
  flet-dev:
    build: .
    container_name: flet_dev_env
    volumes:
      - .:/app
    ports:
      - "8550:8550"
    environment:
      - FLET_SERVER_PORT=8550
      - FLET_SERVER_IP=0.0.0.0
    command: python main.py
    restart: unless-stopped